Program calcularAreaTriangulo
Start
// Programa para calcular el área de un triángulo

// Declaración de variables
Declare Float base
Declare Float altura
Declare Float area

// Obtención de datos
Display "Ingrese la base del triángulo: "
Input base
Display "Ingrese la altura del triángulo: "
Input altura

// Cálculo del área
Set area = (base * altura) / 2

// Presentación de resultado
Display "El área del triángulo es ", area

End
